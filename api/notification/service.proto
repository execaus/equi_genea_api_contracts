syntax = "proto3";

package auth;

import "google/protobuf/empty.proto";

option go_package = "api/notification;notification";

service NotificationService {
  rpc sendCreationAccountEmail(sendCreationAccountEmailRequest) returns (google.protobuf.Empty);
}

message sendCreationAccountEmailRequest {
  string email = 1;
  string password = 2;
}